Building Documentation
======================

We use Sphinx with:

- **Mermaid** diagrams (`sphinxcontrib-mermaid`)
- **Sora** theme + custom logos
- **autosummary** for API stubs
- **copybutton**, **panels**, **tabs** for better UX

Build locally:

.. code-block:: bash

   cd docs
   make html
